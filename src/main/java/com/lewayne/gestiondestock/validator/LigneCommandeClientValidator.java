package com.lewayne.gestiondestock.validator;

import com.lewayne.gestiondestock.dto.LigneCommandeClientDTO;

import java.util.ArrayList;
import java.util.List;

public class LigneCommandeClientValidator {

    // TODO to be implemented
    public static List<String> validate(LigneCommandeClientDTO commandeClientDTO) {
        List<String> errors = new ArrayList<>();
        //on se rassure que la ligneCommandeClient n'est pas null
        // et si l'article ou la liste des article n'est pas null, on verifi donc que l'id de l'article n'est pas null
        return errors;
    }
}
